const React = require('react');
const ReactDOM = require('react-dom');

const Dropdown = React.createClass({
  render: function() {
    return (
      <div className="dropdown">
        Hello, world! I am a Dropdow.
      </div>
    );
  }
});

export default Dropdown;
